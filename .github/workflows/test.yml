# .github/workflows/test.yml
# ===============================================
# MSM GPVファイルを描画 → Slackへ自動投稿テスト
# ===============================================

name: Test

on:
  workflow_dispatch:

jobs:
  test-echo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo start message
        run: echo "START"
